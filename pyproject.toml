[project]
name = "mcp-command"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.2.4",
    "starlette>=1.0.0",
    "uvicorn>=0.46.0",
]
